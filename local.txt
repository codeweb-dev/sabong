STEP 1: config the herd make the variables_order="E---"

STEP 2: my the url like this - based on the ipconfig
APP_URL=http://192.168.100.110

STEP 3: make the reverb like this
REVERB_APP_ID=119680
REVERB_APP_KEY=h3nmkqzdr6j1rhpxnhyx
REVERB_APP_SECRET=josyszt5ttpfv157pgap
REVERB_HOST=192.168.100.110
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="192.168.100.110"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

✅ STEP 4 — Allow port 8080 in Windows Firewall (VERY IMPORTANT)

Open Windows Defender Firewall

Click Advanced Settings
Go to Inbound Rules
Click New Rule
Type: Port
Protocol: TCP
Port: 8080
Allow the connection
Apply to Private + Public
Then click Finish.

🔥 This step is REQUIRED so your phone can connect to Reverb.

STEP 5: Run the system
php artisan serve --host=0.0.0.0 --port=8000
php artisan reverb:start

_

for example
si declator nag declare ng meron may nag payout na meron (2) eh mali pala dapat wala panalo ngayon yun dalawa nag payout sa meron magpupula



DECLATOR SIDE:
    make the close button & open clickable for example that if declarator

TEller SIDE:
    remove the 0 in the input

    short keyboard
    / = meron button
    * = wala button

    will redirect to place bet when show the modal then the ENTER short cut will submit (make it form)

    - (dash) = clear input

ADMIN SIDE:
    add the system over, etc.

    TRANSACTION
     make the received button will count for example the button will have (2)
     add the cancel in received it will redirect the money to the one who send


add short page in user
    sumarry short
    then table

    for example sa declarator may na declare na isang side na meron pero may taya sya na 200 tapos na payout na scan ngayon nag change galing sya na unpaid naging paid

    if declarator change the winner all the previuos payout will make the status to "short" and then the payout_amount will redirect to the new table of user "short"

    remove the red background in betting history
    make the all the price comma
